name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  client:
    name: Client
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: client
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: client/package-lock.json
      - run: npm ci
      - run: node ../.github/scripts/npm-audit.mjs
        env:
          # SPA-only usage; allow the React Router RSC advisory while keeping all other high/critical findings blocking.
          NPM_AUDIT_ALLOW_GHSA: GHSA-QWWW-VCR4-C8H2
      - run: npm run lint
      - run: npm run test:ci
      - run: npm run test:prerender
      - run: node --test ../.github/scripts/deploy-render.test.mjs ../.github/scripts/npm-audit.test.mjs
      - run: npm run build
      - run: npx playwright install --with-deps chromium webkit
      - run: npm run test:e2e

  server:
    name: Server
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: data_explorer_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d data_explorer_ci"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    defaults:
      run:
        working-directory: server
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/data_explorer_ci
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: server/package-lock.json
      - run: npm ci
      - run: node ../.github/scripts/npm-audit.mjs
        env:
          # Prisma 6.19.3 still resolves deepmerge-ts 7.x; npm's only fix is a Prisma 6.12 downgrade.
          # Keep this known transitive CLI/config advisory explicit while retaining all other blockers.
          NPM_AUDIT_ALLOW_GHSA: GHSA-GGR8-5VV4-36MX
      - run: npm run prisma:generate
      - run: npx prisma migrate deploy
      - run: npm run lint
      - run: npm run test:ci
      - run: npm run build

  containers:
    name: Container images
    needs: [client, server]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7
      - name: Build images
        env:
          IMAGE_PREFIX: ghcr.io/${{ github.repository }}
          PRODUCTION_API_URL: ${{ vars.PRODUCTION_API_URL || 'https://data-explorer-editor-9v55.onrender.com' }}
        run: |
          image_prefix="$(echo "$IMAGE_PREFIX" | tr '[:upper:]' '[:lower:]')"
          api_url="${PRODUCTION_API_URL%/}/api"
          docker build --build-arg "VITE_API_URL=$api_url" --tag "$image_prefix/client:${GITHUB_SHA}" --tag "$image_prefix/client:main" client
          docker build --tag "$image_prefix/server:${GITHUB_SHA}" --tag "$image_prefix/server:main" server
          echo "IMAGE_PREFIX=$image_prefix" >> "$GITHUB_ENV"
      - name: Scan images for vulnerabilities (Trivy)
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: ${{ env.IMAGE_PREFIX }}/server:${{ github.sha }}
          format: table
          exit-code: '1'
          ignore-unfixed: true
          vuln-type: os,library
          severity: HIGH,CRITICAL
      - name: Scan client image for vulnerabilities (Trivy)
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: ${{ env.IMAGE_PREFIX }}/client:${{ github.sha }}
          format: table
          exit-code: '1'
          ignore-unfixed: true
          vuln-type: os,library
          severity: HIGH,CRITICAL
      - name: Generate SBOM (server)
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.IMAGE_PREFIX }}/server:${{ github.sha }}
          artifact-name: sbom-server-${{ github.sha }}.spdx.json
          format: spdx-json
      - name: Generate SBOM (client)
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.IMAGE_PREFIX }}/client:${{ github.sha }}
          artifact-name: sbom-client-${{ github.sha }}.spdx.json
          format: spdx-json
      - name: Publish images
        if: github.event_name == 'push'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "$GITHUB_TOKEN" | docker login ghcr.io --username "$GITHUB_ACTOR" --password-stdin
          docker push "$IMAGE_PREFIX/client:${GITHUB_SHA}"
          docker push "$IMAGE_PREFIX/client:main"
          docker push "$IMAGE_PREFIX/server:${GITHUB_SHA}"
          docker push "$IMAGE_PREFIX/server:main"
