{
  "name": "data-explorer",
  "version": "3.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c blue,green \"npm run start:dev --prefix server\" \"npm run dev --prefix client\"",
    "server": "npm run start:dev --prefix server",
    "client": "npm run dev --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "typescript": "^6.0.3"
  }
}
