{
  "name": "data-explorer",
  "version": "3.6.2",
  "private": true,
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c blue,green \"npm run start:dev --prefix server\" \"npm run dev --prefix client -- --host\"",
    "server": "npm run start:dev --prefix server",
    "client": "npm run dev --prefix client -- --host"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "license": "MIT"
}
